//! adblock-rust engine wrapper: builds an `Engine` from filter lists (ABP
//! syntax and hosts files) plus user rules, with a serialized on-disk cache.
//!
//! Phase 0 skeleton — the engine wrapper lands with P1-B/C.
